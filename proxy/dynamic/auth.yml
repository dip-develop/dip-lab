http:
  routers:
    dashboard:
      rule: "Host(`traefik.dipdev.de`)"
      service: api@internal
      entryPoints:
        - websecure
      middlewares:
        - auth_basic
        - security-headers
      tls:
        certresolver: letsencrypt

  middlewares:
    auth_basic:
      basicAuth:
        users:
          - "admin:$apr1$MRfYsEPI$iJgpGkZoJxvKGoXmINehu/"
